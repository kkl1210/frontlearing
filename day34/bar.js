function drawChart(datas) {
    const chartHeight = 400
    const space = 20
    const columnColor = "#B0C4DE"
    const aixsColor = black
    //得到最大值
    let max
    max=datas[0]
    for(let i =0;i<datas.length;i++){
        if(max<datas[i]){
            max =datas[i];
        }
    }
    //数据和像素的转换
    let maxHeight;
    maxHeight = 20 * data
    //绘制横轴和纵轴

    //遍历数据
    for(let i =0;i<sourceData[i].sale.length;i++){
        
    }


}